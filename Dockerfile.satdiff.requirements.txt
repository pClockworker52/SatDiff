# SatDiff Python pipeline runtime deps.
# Pinned loosely (>=); upper bounds left open since we don't have a stale-deps risk in 11 days.

# phase1 — data + physical-diff
numpy>=1.26
scipy>=1.13
xarray>=2024.6
h5netcdf>=1.3
h5py>=3.11    # backend for h5netcdf — not pulled transitively
shapely>=2.0
pillow>=10.0
rasterio>=1.4
requests>=2.32

# phase2 — VLM contract pipeline (llama-server transport only — no torch/transformers)
jsonschema>=4.22

# phase3 — PDF renderer (workstream [1])
jinja2>=3.1
weasyprint>=62.0
