# Phase 0 Go/No-Go Assessment — FINAL

**Status:** Final after Brumadinho + Jagersfontein research.
**Date:** 2026-04-24

## Verdict

> **PROCEED — with Path A (fusion pipeline, Jagersfontein as primary, Brumadinho as secondary/contrast).**

## Summary of evidence

### Brumadinho (`research/brumadinho-memo.md`)
- **Sentinel-2 alone is insufficient.** Pre-failure NDVI was flat-to-rising. Expert-panel drone confirmed no visual distress 7 days before collapse.
- **Primary pre-failure satellite signal is Sentinel-1 SAR deformation** (Grebby et al. 2021, ~40-day lead via ISBAS InSAR).
- Commercial InSAR + GISTM Principle 7 have largely closed the "detect tailings deformation" gap for this class of asset.

### Jagersfontein (`research/jagersfontein-memo.md`)
- **Sentinel-2 is load-bearing and has a clean multi-year precursor story.** Torres-Cruz & O'Donovan 2023 (Wits, *Scientific Reports*): erosion gullies (Feb 2019), persistent pond against wall, asymmetric deposition, absent beach — all detectable in public Sentinel-2 + Landsat 8 imagery from 2015 onwards.
- Pre-failure horizon: **~3.5 years of machine-detectable anomalies** before the 11 Sep 2022 failure.
- Official forensic verdict (UP + Wits, publicly released Nov 2025): **"foreseeable and preventable."**
- Failure mode was regulatory enforcement, not signal absence. DWS issued a cease-deposition directive Dec 2020 then overturned it May 2021 despite unmet conditions.

### Gap assessment (`research/insar-gap-assessment.md`)
Commercial satellite monitoring exists at scale (TRE Altamira × Glencore; Insight Terra × Synspective) and is GISTM-aligned. The defensible SatDiff gap is **not** "detect signals InSAR misses" but three other things:

- **A. Interpretation layer** — translating physical signals into contract-framed, claim-indexed outputs.
- **B. Multispectral + SAR fusion** — no deployed service unifies these into a single per-asset per-claim narrative.
- **C. On-satellite inference for portfolio scale** — the emerging architecture Liquid AI/LEAP is pushing; nothing deployed today does this on-orbit.

## Why Path A wins

Path A (fusion pipeline with Jagersfontein primary, Brumadinho secondary) is the honest fit for what the evidence supports:

1. **Jagersfontein justifies the Sentinel-2 commitment.** A ~3.5-year multi-spectral precursor record is the backtest signal the plan assumes.
2. **Brumadinho justifies the SAR commitment.** If the pipeline claims to work on tailings dams generally, it must handle the case where the signal is deformation-dominant, not multispectral-dominant.
3. **The two cases together make the architectural point.** Modality-agnostic fusion → VLM interpretation → contract-framed output → on-satellite compact report. Two cases with opposite sensor-signal profiles is a stronger architectural demonstration than two cases of the same type.
4. **The counterfactual is defensible for Jagersfontein without the institutional overclaim Brumadinho forces.** At Jagersfontein the satellite signal was public; the enforcement record was the failure. "Machine-generated, time-stamped, contract-framed evidence would have made enforcement waiver harder" is a much more defensible claim than "better monitoring would have saved 270 lives at Brumadinho" (where Vale's institutional response was the real failure mode).
5. **GISTM alignment is natural.** Both cases sit in the direct post-Brumadinho regulatory evolution; the submission writeup can position SatDiff as the VLM-enabled next step after GISTM Principle 7.

## Scope commitments (what Path A requires)

| Commitment | Scope impact |
|------------|--------------|
| Sentinel-1 SAR ingest (at least for Brumadinho Claim 1) | Medium. Plan for the hackathon: use **published ISBAS time series from Grebby et al.** as structured input rather than doing our own ISBAS processing. If time permits, add a simple SBAS/PSI processing of the Sentinel-1 archive. |
| Jagersfontein pond-to-wall geometry computation | Low. NDWI segmentation + distance-transform against retaining-wall polygon. |
| Gully-detection proxy | Low-medium. Simple: change-detection on RGB + NIR edge responses on the wall mask. Deep enough to show "non-zero gully count, widest feature width" for the VLM. |
| Asymmetric-deposition index | Low. Time-series differencing of fill-advance rate per sector. |
| Contract-prompt package with 5 claims | Already drafted (`research/contract-prompts/*`). Needs refinement in Phase 2. |
| On-satellite VLM inference spike (LEAP + LFM2) | Must be done early — Phase 1 or start of Phase 2 — not Phase 2.5. If LFM2 vision capability is insufficient for multi-image + structured-JSON output, Path A needs reassessment. |

## What the submission writeup must NOT claim (applies under any path)

- Not: "SatDiff would have detected Brumadinho from Sentinel-2 alone."
- Not: "SatDiff would have prevented the loss of life at Brumadinho / Jagersfontein." (Monitoring ≠ enforcement ≠ action.)
- Not: "SatDiff replaces commercial InSAR services." (It doesn't; it complements them at the interpretation + on-edge layer.)

What it *can* claim with evidence:

- "At Jagersfontein, public Sentinel-2 + Landsat imagery carried a multi-year precursor signal (Torres-Cruz & O'Donovan 2023, *Scientific Reports*). SatDiff demonstrates how that signal becomes machine-generated, time-stamped, contract-framed output at the moment of acquisition on-orbit."
- "At Brumadinho, Sentinel-1 SAR deformation carried a ~40-day precursor signal (Grebby et al. 2021, *Communications Earth & Environment*). SatDiff demonstrates the fusion-and-interpretation architecture that turns such signals into insurer/regulator-ready per-claim assessments."
- "The architectural pattern — on-satellite physical-diff + VLM + ground-supervisor — enables portfolio-scale continuous monitoring at a marginal downlink cost that current deployed services cannot match. Liquid AI's LEAP + LFM2 is the enabling technology for this pattern."

## Next steps (Phase 1 and the LEAP spike)

Per the plan's pre-Phase-0 commitments, two spikes should happen before serious Phase 1 build:

1. **LEAP + LFM2 access spike** (laptop). Goal: confirm `inference on (2 images, structured text, prior context) → structured JSON` works at acceptable latency. Output: a working notebook + a go/no-go on LFM2 vision capability for this task.
2. **Sentinel-2 + Sentinel-1 archive-pull spike** (laptop). Goal: pull a test slice for Jagersfontein (say, a 6-month window) via Microsoft Planetary Computer STAC API. Confirm tile identification, cloud-cover filtering, co-registration path. Output: a working data-loader + first visual sanity-check of the Torres-Cruz pond-against-wall signal in our own pull of the archive.

If both spikes succeed, Phase 1 begins with Jagersfontein as primary. If the LEAP spike fails (LFM2 vision insufficient), the whole programme needs reassessment per the plan's Phase-2.5 kill criterion escalated to Phase 0.

## Decision log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-24 | Brumadinho memo drafted. Provisional: Sentinel-2-only path not defensible for this case. | Grebby 2021 uses Sentinel-1 ISBAS; Sentinel-2 NDVI shows no pre-failure signal. |
| 2026-04-24 | Jagersfontein memo drafted. Clean Sentinel-2 case. | Torres-Cruz & O'Donovan 2023 documents 3.5 years of public multispectral precursors; official forensic report calls failure "foreseeable and preventable." |
| 2026-04-24 | **PATH A confirmed: fusion pipeline, Jagersfontein primary, Brumadinho secondary.** | Two cases with opposite sensor-signal profiles is the strongest architectural demonstration the evidence supports. |
| 2026-04-24 | Next: LEAP/LFM2 access spike + data-pull spike. Hold on Phase 1 build work until both succeed. | Plan's pre-Phase-0 commitments. |
| 2026-04-25 | **Spike 1 result: VIABLE with caveats. Path A continues.** Base LFM2.5-VL-450M sees the imagery (free-text mode correctly localises pond, town, impoundment in the 2021-12-30 Jagersfontein scene); under the full contract prompt it produces correct structure with plausible severity grading (Claim 2 pond-management correctly elevated, 4/5 plausibly graded) but parrots the contract's "Sensors" lines into `evidence` instead of describing imagery. Latency 6.5 s, peak VRAM 0.97 GB on RTX 4080. See `research/spike-1-findings.md`. | Imagery-grounding capability confirmed; the contract-prompt evidence gap is a prompt-engineering / fine-tuning problem (Phase 2 + Spike 3 / Day 6), not a base-capability kill. |
| 2026-04-25 | Phase 2 prompt pipeline must use a two-stage decode (free-text scene description → JSON shaping). Lift this from Phase 2.5b agentic pattern into the main Phase 2 design. | 450M's format-matching reflex dominates imagery-grounding under heavy structured-output prompts; two-stage decode tested working in spike's targeted change-detection variant. |
| 2026-04-25 | Spike 3 / Day 6 fine-tuning gains weight: it is now also load-bearing for making contract-schema output reliable, not just a rubric extra. Stage 2 SatDiff-specific custom set should target the contract-schema output format directly. | Per Spike 1: even with explicit `[OUTPUT INSTRUCTIONS]` the base model's parrot behaviour is greedy-deterministic; in-distribution training is the principled fix. |
| 2026-04-25 | **Correction to earlier note:** Element 84 STAC works fine. The Spike 1 "empty results" was a malformed-request issue (the `query=` filter extension does not work via GET on Element 84's v1 stac-server; `pystac-client` uses POST internally and returns tiles in <1 s). SimSat's stock Element 84 backend is expected to work as-shipped. | Diagnostic re-run via `pystac-client` (the same lib SimSat uses) returned tiles for Jagersfontein 2022-01, London 2024-09, and `sentinel-2-c1-l2a` Jagersfontein 2024-09. The MSPC fallback in `spikes/leap-vlm-check.py` is retained as defence-in-depth but is no longer required. |
| 2026-04-25 | **Spike 2 result: VIABLE. Path A's criterion-1 (DPhi API ingress) confirmed.** Stock SimSat against Element 84 returned 4/5 Torres-Cruz milestone dates and 17/17 months in a rolling 2021-06 → 2022-10 window for Jagersfontein, median latency 5.02 s. The 2022-09 query autonomously surfaced the failure day (2022-09-11). Step 2b MSPC patch was not needed. See `research/spike-2-findings.md`. | Three small SimSat-side gotchas to bake into the submission Docker image: (a) `.dockerignore` skipping `src/dashboard/frontend/node_modules` (BuildKit symlink-mode bug); (b) `.env` setting `MAPBOX_ACCESS_TOKEN=anything` (eager init in `api.py` crashes if unset); (c) PNG mode rejects multispectral, so multispectral pipeline calls `return_type=array`. Element 84's `sentinel-2-l2a` lacks pre-2017 archive for T35JLH — Phase 1 backtest can either use 2017+ baseline (Torres-Cruz signal starts Feb 2019) or fall through to `sentinel-2-pre-c1-l2a`. |
| 2026-04-25 | **Spike 3 result: VIABLE. Day 6 fine-tune runs locally on the RTX 4080.** `Liquid4All/leap-finetune` smoke job (LFM2.5-VL-450M + LoRA on a 16-train-sample example dataset) completed in 32 s with eval_loss 3.22, train_loss 3.32, no OOM, exit 0; merged fp16 checkpoint persisted automatically. `uv sync` cost ~27 min one-time on a fresh ext4 venv (Python 3.12, torch 2.9.1+cu128, transformers 5.2.0, flash-attn 2.8.3 built from source in 1m32s). Modal stays as registered fallback for the published reproducible run. See `research/spike-3-findings.md`. | Day 6 sub-samples VRSBench (~5K examples × 2 epochs ≈ 5–6 h) for Stage 1 + ~30 SatDiff custom examples for Stage 2. Framework writes a merged fp16 checkpoint loadable by `spikes/leap-vlm-check.py` directly — no LEAP-specific tooling needed for inference. Custom benchmarks plug into the YAML `benchmarks:` section using JSONL files. The recipe-mentioned `vrsbench_multitask_modal.yaml` does NOT ship with the repo at this HEAD; build a config from the shipped `vlm_sft_example.yaml`. **All three pre-Phase-1 spikes pass — Phase 1 build can begin.** |
| 2026-04-25 | **Phase 1 result: VIABLE. Pipeline is producing a Torres-Cruz-aligned signal trajectory across 17 months.** Loader (SimSat array-mode → xarray NetCDF cache), masks (Torres-Cruz cross-referenced v1.1 polygons), baseline indices (2017-10-15, pond_area 90,689 m²), physical-diff (per-mask metrics matching contract memo schema), gate (Φ-sat-heritage cloud + interest filter), and CLI bulk-runner all done. 17/17 cached SimSat tiles processed; 16/17 invoke the VLM. Headline trajectory: pond grows from 90 k m² (2017) → 638 k m² (2021-12) → 1,897 k m² (2022-01) → 3,272 k m² post-failure (2022-10). Pond-to-wall distance is 0 or 9.9 m (one pixel) for 14/17 months. Asymmetry index spikes 1.5 → 22.6 across the 2022-09 failure month. See `research/phase-1-findings.md`. | One real defect: the 2022-09-11 failure day pass (68% cloud) was skipped by the gate. Mitigations are part of the writeup's production-roadmap section (extreme-metric override; SAR sidecar for cloud days). Mask edge accuracy is ±~20 m so the writeup reports ratios/trends, not raw m² to 4 s.f. **Phase 2 (LFM2.5-VL prompt pipeline) is unblocked.** |
| 2026-04-26 | **Phase 2 result: VIABLE. LFM2.5-VL prompt pipeline + Python rules engine produces schema-valid contract JSON for 16/16 invoked passes.** Two-stage decode (Stage A free-text describe → Stage B contract JSON) plus a deterministic `phase2/aggregate.py` rules engine that overlays severity_level / recommended_action / overall_status / downlink_priority / regulatory_escalation_flag onto the model's parsed output. 16/16 schema valid; 16/16 evidence cites at least one diff metric value; 16/16 overall=urgent (matches Torres-Cruz: continuous covenant violation). Latencies: Stage A 3.95 s mean, Stage B 7.24 s mean, ~11.2 s/pass total. Peak VRAM 1.11 GB. Total severity_corrections (model vs rules engine): 12 across 16 passes; model agreed on every claim in 8/16 passes — the canonical gap is the 450M's chained threshold logic (pond_to_wall<25→urgent, gully_count≥10→urgent), the exact brief for Day 6 fine-tune. See `research/phase-2-findings.md`. | Caught and fixed a backtest-integrity bug mid-build: an earlier draft of the Stage A prompt mentioned the 2022-09-11 catastrophic dam breach by date and location; this leaks future information into every backtest pass. Replaced with neutral routine-monitoring framing; audited `phase1/loader.py::ASSETS` description and added a `_prompt_safety` guard on the mask GeoJSON. The architectural decision to move severity rules out of the prompt and into Python is honest about the v1 model's reasoning ceiling and matches how production audit systems separate "writer" from "rules engine". Phase 3 (PDF rendering / demo wiring + Day 6 fine-tune) is unblocked. |
| 2026-04-26 | **Day 6 Stage 1 fine-tune result: VIABLE, headline result.** 5K VRSBench captions+VQA (no `[refer]`) × 2 epochs LoRA on LFM2.5-VL-450M, 38m 15s on RTX 4080. eval_loss 3.21 → 1.41 (−56%). Re-running Phase 2 backtest with the merged fp16 ckpt: schema_valid 16/16, **evidence-correct claims 4.7% → 100%** (the base model was citing wrong diff fields for 95% of claims — Stage 1 alone, with no SatDiff-specific examples, taught the model to follow per-claim sourcing rules). severity_corrections unchanged (12 → 12) — Stage 1 targets generic EO grounding, not multi-tier threshold logic. See `research/phase-3-findings.md` (or in-line in CLAUDE.md "Day 6 fine-tune — Stage 1"). | Stage 1 ckpt at `/home/peter/leap-finetune/outputs/satdiff_stage1/.../LFM2.5-VL-450M-vlm_sft-satdiff_st-all-lr1em05-w0p2-lora_m-20260426_221338` (861 MB, fp16). The 0% → 100% lift is *real OOD generalization* (training was VRSBench, eval was Jagersfontein backtest — fully disjoint). |
| 2026-04-27 | **Stage 2 hand-authored dataset: 29 examples sign-off-ready.** 11 Tier 1 boundary (synthetic threshold cliffs with rotating Jagersfontein imagery) + 5 confab override + 5 escalation chain + 4 Tier 2 routine (Aswan / Three Gorges / Hoover-Mead / Kariba — paper-grounded, imagery-derived NDWI metrics) + 4 Tier 3 catastrophic (Brumadinho / Toddbrook / Derna / Nova Kakhovka — paper-cited quantitatives, manual severity overrides for non-Jagersfontein asset classes). Mariana skipped (redundant with Brumadinho); Oroville skipped (no clean S2 imagery shows distress); Edenville dropped during review (S2 tile-boundary + Wixom Lake didn't pass NDWI). User reviewed via custom HTML homepage at `/home/peter/datasets/satdiff_stage2/review.html`; flagged two issues that were fixed. Plan: `~/.claude/plans/let-s-start-with-research-polished-flame.md` (Stage 2 v3 the v1 of this plan file iteration). | Discord skim revealed competitive landscape: Liquid's wildfire-prevention (canonical reference, GGUF + llama-server), DPhi's maritime-monitoring (agentic, Ever Given), specialised Sentinel-2 datasets being published. Used to inform writeup positioning. |
| 2026-04-28 | **Stage 2 result: NEGATIVE on the right metric (severity adherence) — but methodologically clean.** First Stage 2 eval was contaminated (eval data was in training set) and showed neutral 12 → 12. Rebuilt with a proper held-out split (35 train / 11 test, 6 backtest dates + 5 hand-authored pass IDs excluded from training via `training/build_stage2_split.py`). Retrained Stage 2 in 1m 37s. Held-out eval: schema_valid 6/6, **evidence_correct preserved at 100%**, severity_corrections actually went **5 → 9 (worse)**. LoRA at 35-train-example × 3-epoch scale cannot reshape multi-tier threshold reasoning; synthetic boundary cliffs (p2w 24/26) don't transfer to held-out real-data input distributions (p2w 9.9 throughout). See CLAUDE.md "Day 6 fine-tune — Stage 2". | **Stage 1 ships, not Stage 2.** Validates the rules-engine architecture: the model is good at the part fine-tuning lifts (evidence text), bad at the part fine-tuning can't fix (multi-tier thresholds). Production pattern = VLM evidence writer + Python rules engine. Memory saved: `feedback_eval_holdout_discipline.md` (always hold out a clean test set BEFORE fine-tune evaluation). Stage 2 ckpt NOT uploaded to HF. |
| 2026-04-28 | **Plan v2 approved for final submission deliverables.** Six workstreams, critical path **[6] → [2] → [4]**: GGUF Q8_0 + F16-mmproj quantize → llama-server inference (replaces Python transformers + bf16 deployment path; cited 10× latency improvement per Liquid wildfire-prevention reference) → Top-level Docker compose (4 services: simsat / llama-server / satdiff / demo-ui) → 5-minute presenter-on-camera demo video. Parallel-able: PDF report renderer, pitch / writeup, HF upload (fp16 + GGUF pair). Total ~4.5 working days; ~6.5 days slack. Plan file: `~/.claude/plans/let-s-start-with-research-polished-flame.md`. | Discord competitive-landscape skim caught two directional inputs: (a) wildfire-prevention is the canonical Liquid reference everyone uses → adopting GGUF + llama-server stack; (b) DPhi's own published reference uses *agentic* maritime monitoring → SatDiff explicitly positions as *non-agentic / contract-framed* in the writeup because GISTM auditor's required artefact is a per-claim gold-standard report, not an investigation transcript. Memory saved: `feedback_check_canonical_sources.md` (always check vendor canonical refs + competitor channels before finalising plans). |
| 2026-04-28 | **Session handoff (~700K tokens consumed).** Stage 2 closed cleanly, plan v2 approved, no implementation started. Next session begins at workstream **[6] GGUF + llama-server**. Source ckpt path, target output paths, and verification criteria all documented in CLAUDE.md "Where the next session starts" + `research/session-2026-04-28-handoff.md`. | Open questions parked: multiple-entry hackathon rule (no Discord answer; default = one); Pau Labarta Bajo's YouTube recording (https://www.youtube.com/watch?v=LOIDYl5fdb8, ~25 min) not yet watched — non-blocking but worth checking before [6] kickoff for any LFM2.5-VL-specific quantization nuances. |
